include:
  - path: ./airflow/compose.yml
  - path: ./flink/compose.yml
  - path: ./kafka/compose.yml
  - path: ./metabase/compose.yml
  - path: ./minio/compose.yml
  - path: ./postgres/compose.yml

volumes:
  postgres-data:
  minio-data:
  kafka-data:

networks:
  weather-net:
    driver: bridge
